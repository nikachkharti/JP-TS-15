using JP_TS_15_MainConsoleProject;

int[] intArray = { 1, 2, 3, 1, 2, -12 };
//string[] names = { "Nika", "Giorgi", "Daviti", "Daviti" };

//var result = Algorithm.FindAll(names, x => x.EndsWith("i"));
//var result = Algorithm.FindAll(names, x => x[x.Length - 1] == 'i');


//string[] vehiclesAsString = File.ReadAllLines(@"../../../vehicles.csv");
//Vehicle[] vehicles = Algorithm.Select(vehiclesAsString, Vehicle.Parse);
////var mercedeses = Algorithm.FindAll(vehicles, x => x.Make.Contains("Mercedes"));
//var economicCars = Algorithm.OrderBy(vehicles, (x, y) => x.Combined > y.Combined);
//var topEconomicCars = Algorithm.Take(economicCars, 10);








